apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: sample-tomcat
  labels:
    app: sample-tomcat
spec:
  path: /
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  to:
    kind: Service
    name: sample-tomcat
    weight: 100
  port:
    targetPort: http
